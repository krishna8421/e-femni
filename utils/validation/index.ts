import { login } from "./userLoginSchema";
import { register } from "./userRegisterSchema";
import { product } from "./productSchema";

export const validationSchema = {
  login,
  register,
  product,
};
